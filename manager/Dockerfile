# 已迁移至仓库根目录统一镜像，请使用：
#   docker build -f ../Dockerfile -t xlangai:latest ..
#   docker compose -f ../docker-compose.yml up -d
#
# 本文件保留仅为兼容旧文档链接；新部署请使用根目录 Dockerfile。

FROM node:22.21.1-alpine3.22
WORKDIR /app
RUN echo "请使用仓库根目录 Dockerfile 构建统一镜像（manager + server）" && exit 1
